version: "3"

services:
    appdhiv:
        build: .
        container_name: appdhiv
        ports:
            - "80:80"
        volumes:
            - .:/var/www/html
networks:
    default:
        name: db_default
        external: true